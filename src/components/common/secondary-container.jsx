import React from "react";

const SecondaryContainer = ({children}) => {
    return (
        <div className="secondary-container width-12 grid-12">
            {children}
        </div>
    );
}

export default SecondaryContainer;